name: Daily Price Scraper

on:
  schedule:
    # Run at 2:00 AM and 2:00 PM IST (Indian Standard Time)
    # IST is UTC+5:30. So 08:30 UTC = 2:00 PM IST, and 20:30 UTC = 2:00 AM IST.
    - cron: '30 8,20 * * *'
  # Allow manual trigger from the GitHub Actions UI
  workflow_dispatch:

jobs:
  scrape-prices:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        
    - name: Install Dependencies
      working-directory: ./scrapers
      run: npm install
      
    - name: Install Playwright Browsers
      working-directory: ./scrapers
      run: npx playwright install chromium --with-deps
      
    - name: Run Scraper Script
      working-directory: ./scrapers
      env:
        NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      run: node index.js

#    - name: Trigger Price Alerts
#      working-directory: ./scrapers
#      env:
#        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
#        SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
#        GMAIL_USER: "sanjoydutta1200@gmail.com"
#        GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
#      run: node trigger-alerts.js
